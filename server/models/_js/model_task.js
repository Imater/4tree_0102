// Generated by CoffeeScript 1.6.3
(function() {
  var Schema, Task, mongoosastic, mongoose, treeSchema;

  mongoose = require("mongoose");

  Schema = mongoose.Schema;

  treeSchema = new Schema({
    'tree_id': String,
    'user_id': {
      type: String,
      es_indexed: true
    },
    'del': {
      type: Number,
      "default": 0
    },
    'title': {
      type: String,
      es_indexed: true
    },
    'date1': Date,
    'date2': Date,
    'parent_id': String,
    '_sha1': String,
    '_sync': [
      {
        key: String,
        diff: {
          '_tm': String
        }
      }
    ],
    '_tm': Date
  });

  mongoosastic = require('mongoosastic');

  treeSchema.plugin(mongoosastic);

  Task = module.exports = mongoose.model("Task", treeSchema);

}).call(this);
